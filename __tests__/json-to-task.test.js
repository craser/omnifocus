const JsonToTask = require('json-to-task');

/**
 * TODO:
 *   - mock Application
 *   = mock OmniFocus
 *   - mock every damn thing
 */


test('not null', () => {
    expect(JsonToTask).not.toBeNull();
})
